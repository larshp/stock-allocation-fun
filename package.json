{
  "name": "stock-allocation-fun",
  "version": "1.0.0",
  "private": true,
  "description": "Stock allocation solution in ABAP, linted with abaplint and tested via transpilation to JavaScript",
  "scripts": {
    "lint": "abaplint abaplint.json",
    "transpile": "abap_transpile abap_transpile.json",
    "test": "npm run lint && npm run transpile && node output/index.mjs"
  },
  "devDependencies": {
    "@abaplint/cli": "^2.113.0",
    "@abaplint/transpiler-cli": "^2.10.0"
  },
  "dependencies": {
    "@abaplint/runtime": "^2.10.0"
  }
}
